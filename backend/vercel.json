{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd ../frontend && npm ci --include=dev && npm run build && mkdir -p ../backend/public && cp -r dist/frontend/browser/* ../backend/public/ && cd ../backend && npm run build",
  "installCommand": "npm ci",
  "outputDirectory": ".next",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!_next|.*\\..+).*)", "destination": "/index.html" }
  ]
}
